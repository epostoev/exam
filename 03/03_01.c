#include <stdio.h>
#include <math.h>
int main() { double r = 0, m = 0, V =0;
    int res = scanf("%lf", &r);
    char last_char = getchar();
    if (res == 1 && last_char == '\n'){V = (4.0 / 3.0) * M_PI * pow(r, 3);
        printf("%.0lf", m = 21500 * V);
    }
    else { printf("n/a");
    }
    return 0;
}
