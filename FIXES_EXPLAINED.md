# Исправления в вашем Makefile

## 🔴 Найденные ошибки:

### Ошибка 1: Неправильное имя цели

```makefile
# ❌ БЫЛО (неправильно):
TARGET_PATH: $(OBJ_DIR) $(BIN_DIR) $(OBJ_FILES)
	$(CC) $(CFLAGS) $^ -o $(BIN_DIR)/$@ $(LDFLAGS)

# ✅ СТАЛО (правильно):
$(TARGET_PATH): $(LIB_TETRIS) $(OBJ_FILES) | $(BIN_DIR)
	$(CC) $(CFLAGS) $(OBJ_FILES) $(LIB_TETRIS) -o $@ $(LDFLAGS)
```

**Проблемы:**
1. `TARGET_PATH:` → должно быть `$(TARGET_PATH):` (с `$`)
2. Не используется библиотека `$(LIB_TETRIS)` в зависимостях
3. `$(BIN_DIR)/$@` неправильно - `$@` уже содержит полный путь
4. `$^` включает директории, что неправильно

**Правильная версия:**
- `$(TARGET_PATH)` = `build/bin/tetris` (переменная раскрывается)
- Зависимости: библиотека + объектные файлы
- `| $(BIN_DIR)` = order-only prerequisite (создать директорию)
- `-o $@` = `-o build/bin/tetris` (полный путь)

---

### Ошибка 2: Нет определения OBJ_FILES

```makefile
# ❌ БЫЛО (отсутствует):
# Object files


# ✅ СТАЛО (добавлено):
OBJ_FILES := \
	$(OBJ_DIR)/main.o \
	$(OBJ_DIR)/gui/cli/cli.o
```

**Проблема:**
Переменная `OBJ_FILES` использовалась, но не была определена.

**Решение:**
Явно перечислить объектные файлы (кроме tetris.o, который в библиотеке).

---

### Ошибка 3: Отсутствие правила для создания библиотеки

```makefile
# ❌ БЫЛО (отсутствует):
#TODO сделать цель на сборку статической библиотеки tetris.a

# ✅ СТАЛО (добавлено):
$(LIB_TETRIS): $(OBJ_TETRIS)
	@echo "Creating static library: $@"
	$(AR) $(ARFLAGS) $@ $^
	@echo "✓ Library created"

$(OBJ_TETRIS): $(SRC_TETRIS) $(HDR_TETRIS) $(HDR_API)
	@echo "Compiling library: $<"
	$(CC) $(CFLAGS) -c $< -o $@
```

**Что добавлено:**
1. Правило для создания `tetris.a` из `tetris.o`
2. Правило для компиляции `tetris.c` в `tetris.o`
3. Команда архивации: `ar rcs tetris.a tetris.o`

---

### Ошибка 4: Отсутствие pattern rule для компиляции

```makefile
# ❌ БЫЛО (отсутствует):
# Нет правила для компиляции .c → .o

# ✅ СТАЛО (добавлено):
$(OBJ_DIR)/%.o: %.c | $(OBJ_DIR)
	@mkdir -p $(dir $@)
	@echo "Compiling: $<"
	$(CC) $(CFLAGS) -c $< -o $@
```

**Что делает:**
- `%` = wildcard (подходит для любого файла)
- `$(OBJ_DIR)/%.o` = цель (например `build/obj/main.o`)
- `%.c` = зависимость (например `main.c`)
- `| $(OBJ_DIR)` = создать директорию если не существует
- `@mkdir -p $(dir $@)` = создать поддиректории

---

### Ошибка 5: Неполная цель clean

```makefile
# ❌ БЫЛО (неполное):
clean:
	rm a.out

# ✅ СТАЛО (полное):
clean:
	@echo "Cleaning build files..."
	@rm -rf $(BUILD_DIR)
	@rm -f $(LIB_TETRIS) $(OBJ_TETRIS)
	@rm -f $(TEST_OBJECTS)
	@rm -f a.out
	@rm -f *.gcda *.gcno *.gcov
	@echo "✓ Clean complete"
```

**Что добавлено:**
- Удаление всей директории `build/`
- Удаление библиотеки и объектников
- Удаление файлов покрытия
- Безопасные флаги: `-rf` для директорий, `-f` для файлов

---

### Ошибка 6: Отсутствие целей test и gcov_report

```makefile
# ❌ БЫЛО (пусто):
test:
#TODO Сделать цель на покрытие и на тест

gcov_report:

# ✅ СТАЛО (реализовано):
test: $(TEST_TARGET)
	@$(TEST_TARGET)

$(TEST_TARGET): $(TEST_OBJECTS) $(LIB_TETRIS)
	$(CC) $(CFLAGS) $(TEST_OBJECTS) $(LIB_TETRIS) \
		-o $@ $(TESTFLAGS) $(LDFLAGS)

gcov_report: CFLAGS += $(GCOVFLAGS)
gcov_report: clean test
	lcov --capture -d . -o coverage.info
	genhtml coverage.info -o $(GCOV_DIR)
```

---

## ✅ Что добавлено:

### 1. Переменные

```makefile
AR := ar
ARFLAGS := rcs
TESTFLAGS := -lcheck -lm -lpthread -lrt -lsubunit
GCOVFLAGS := -fprofile-arcs -ftest-coverage
TARGET := tetris
TARGET_PATH := $(BIN_DIR)/$(TARGET)
```

### 2. Новые цели

- `$(LIB_TETRIS)` - сборка статической библиотеки
- `$(TARGET_PATH)` - сборка исполняемого файла
- `$(OBJ_DIR)/%.o` - pattern rule для компиляции
- `$(CLANG_FORMAT_FILE)` - создание .clang-format
- `test` - запуск тестов
- `gcov_report` - отчёт о покрытии
- `run` - запуск игры
- `rebuild` - пересборка
- `help` - справка

### 3. Комментарии с объяснениями

Все TODO превращены в подробные комментарии с объяснениями:
- Что делает `@mkdir -p $(dir $@)`
- Что делает `$(CC) $(CFLAGS) -c $< -o $@`
- Как работает статическая библиотека
- Как создаётся .clang-format

---

## 📊 Сравнение: было → стало

| Было | Стало |
|------|-------|
| `TARGET_PATH:` | `$(TARGET_PATH):` |
| Нет `OBJ_FILES` | `OBJ_FILES` определено |
| Нет правила для библиотеки | Правило добавлено |
| Нет pattern rule | Pattern rule добавлено |
| `clean` удаляет только a.out | `clean` удаляет всё |
| `test` пустая | `test` реализована |
| `gcov_report` пустая | `gcov_report` реализована |
| Нет комментариев | Подробные объяснения |

---

## 🎯 Как работает итоговый Makefile

### Структура зависимостей:

```
all
 ├── tetris.a (статическая библиотека)
 │    └── tetris.o
 │         └── tetris.c
 └── build/bin/tetris (исполняемый файл)
      ├── tetris.a
      ├── build/obj/main.o
      │    └── main.c
      └── build/obj/gui/cli/cli.o
           └── gui/cli/cli.c
```

### Процесс сборки:

```bash
make all

# Шаг 1: Компиляция tetris.c → tetris.o
gcc -std=c11 -Wall -Werror -Wextra -pedantic -c brick_game/tetris/tetris.c -o brick_game/tetris/tetris.o

# Шаг 2: Создание библиотеки tetris.a
ar rcs brick_game/tetris/tetris.a brick_game/tetris/tetris.o

# Шаг 3: Компиляция main.c → build/obj/main.o
gcc -std=c11 -Wall -Werror -Wextra -pedantic -c main.c -o build/obj/main.o

# Шаг 4: Компиляция cli.c → build/obj/gui/cli/cli.o
gcc -std=c11 -Wall -Werror -Wextra -pedantic -c gui/cli/cli.c -o build/obj/gui/cli/cli.o

# Шаг 5: Линковка в исполняемый файл
gcc -std=c11 -Wall -Werror -Wextra -pedantic \
    build/obj/main.o \
    build/obj/gui/cli/cli.o \
    brick_game/tetris/tetris.a \
    -o build/bin/tetris \
    -lncurses
```

---

## 🚀 Использование

```bash
# Сборка
make all          # Библиотека + исполняемый файл
make              # То же самое (по умолчанию)

# Очистка
make clean        # Удалить build/
make fclean       # Полная очистка

# Тестирование
make create-tests # Создать шаблон тестов
make test         # Запустить тесты
make gcov_report  # Отчёт о покрытии

# Форматирование
make format       # Отформатировать код
make format-check # Проверить форматирование

# Запуск
make run          # Собрать и запустить игру

# Справка
make help         # Показать все цели
```

---

## ✅ Проверка правильности

После установки нового Makefile:

```bash
# 1. Очистка
make clean

# 2. Сборка
make all

# Должно быть:
# ✓ brick_game/tetris/tetris.a создан
# ✓ build/bin/tetris создан
# ✓ Нет ошибок компиляции

# 3. Запуск
make run
# Игра должна запуститься
```

---

## 📚 Полезные ссылки

- Автоматические переменные Make: `$@`, `$<`, `$^`
- Функции Make: `$(dir ...)`, `$(addprefix ...)`
- Pattern rules: `%.o: %.c`
- Order-only prerequisites: `| $(DIR)`

---

Теперь ваш Makefile полностью рабочий и хорошо документированный! 🎉
